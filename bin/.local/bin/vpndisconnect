#!/bin/bash
conn=$(nmcli -t -f name,type connection show --active | grep vpn | cut -d':' -f 1)

if [ -z $conn ]; then
    notify-send -t 5000 -u critical "Error" "Não há conexão ativa"
else
    result=$(nmcli connection down $conn 2>&1)
    notify-send "Success" "$result"
fi
